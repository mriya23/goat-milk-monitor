---
// Client-side only chart component
interface Props {
	id: string;
	height?: string;
}

const { id, height = '300px' } = Astro.props;
---

<div style={`position: relative; height: ${height};`}>
	<canvas id={id}></canvas>
</div>
